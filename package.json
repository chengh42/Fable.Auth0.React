{
  "scripts": {
    "pretest": "fable-splitter tests -o dist/tests --commonjs",
    "test": "mocha dist/tests"
  },
  "devDependencies": {
    "fable-splitter": "^2.2.1",
    "mocha": "^9.2.0"
  }
}
